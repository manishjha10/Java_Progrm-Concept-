public class Main{
  static void merge(int[]  a , int[]  b) { 
  int i = a.length-1 , j = 0;
    while(i >= 0 && j < b.length) {
      if(a[i > b[j{
         int temp = arr[i] ;
         arr[i] = arr[j] ;
           arr[j] = temp ;   //  b array ko maximum banana hai in compare to a array 
  else{
    i -- ;
    j++; }
  }
    Arrays.sort(a) ;
    Arrays.sort(b) ;
  }
}
  
